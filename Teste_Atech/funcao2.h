#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <string.h>
#include "Exercicio2.c"





int funcao(char main1()) {

    char STR = main1();

    if (STR == "(hello (world))")
        return 1;
    else
    if (STR == "(hello (world)")
        return 1;
    else
    if (STR == "(hello world)")
        return 1;
    else
    if (STR == "(hello world")
        return 1;
    else
    if (STR == "hello world")
        return 1;
    else
    if (STR == "hello world)")
        return 1;
}